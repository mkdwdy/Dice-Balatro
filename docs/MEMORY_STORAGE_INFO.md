# 메모리 스토리지 사용 가이드

## ✅ 완료된 작업

데이터베이스 없이 서버를 실행할 수 있도록 **메모리 스토리지**를 추가했습니다!

## 🎯 변경 사항

1. **InMemoryStorage 클래스 추가**
   - 데이터베이스 없이 메모리에 게임 세션 저장
   - 서버 재시작 시 데이터는 초기화됨 (개발/테스트용)

2. **자동 스토리지 선택**
   - `DATABASE_URL`이 설정되어 있으면 → `DatabaseStorage` (PostgreSQL)
   - `DATABASE_URL`이 없으면 → `InMemoryStorage` (메모리)

## 🚀 서버 실행 방법

### 방법 1: DATABASE_URL 없이 실행 (메모리 스토리지)

```bash
cd /Users/dwmoon/Downloads/Dice-Balatro

# 환경 변수 설정 (DATABASE_URL 제외)
export PORT=5000
export NODE_ENV=development

# 서버 실행
npm run dev
```

### 방법 2: .env 파일 사용

`.env` 파일을 만들지 않거나, DATABASE_URL만 제외하면 메모리 스토리지를 사용합니다:

```bash
# .env 파일 생성 (DATABASE_URL 없이)
cat > .env << 'EOF'
PORT=5000
NODE_ENV=development
EOF

# 서버 실행
npm run dev
```

## 📝 특징

### 메모리 스토리지 (InMemoryStorage)
- ✅ 데이터베이스 설치 불필요
- ✅ 빠른 시작
- ✅ 개발/테스트에 적합
- ⚠️ 서버 재시작 시 데이터 초기화
- ⚠️ 프로덕션 환경에는 부적합

### 데이터베이스 스토리지 (DatabaseStorage)
- ✅ 데이터 영구 저장
- ✅ 프로덕션 환경에 적합
- ⚠️ PostgreSQL 설치 필요
- ⚠️ DATABASE_URL 설정 필요

## 🧪 테스트

서버가 실행되면:

```bash
# 새 게임 생성
curl -X POST http://localhost:5000/api/games/new

# 응답 예시:
# {
#   "id": "uuid-here",
#   "health": 100,
#   "gold": 0,
#   ...
# }
```

## 💡 주의사항

- 메모리 스토리지는 **개발/테스트용**입니다
- 서버를 재시작하면 모든 게임 데이터가 사라집니다
- 프로덕션 환경에서는 반드시 `DATABASE_URL`을 설정하세요

## 🔄 데이터베이스로 전환

나중에 PostgreSQL을 사용하려면:

```bash
# DATABASE_URL 설정
export DATABASE_URL="postgresql://postgres:password@localhost:5432/dice_balatro"

# 서버 실행 (자동으로 DatabaseStorage 사용)
npm run dev
```

## ✅ 확인

서버가 성공적으로 시작되면:
- 브라우저: http://localhost:5000
- API: http://localhost:5000/api/games/new

로그에서 다음 메시지를 확인할 수 있습니다:
```
serving on port 5000
```

